@model List<DMS.Sample.Contracts.UserEntity>
@{
    ViewData["Title"] = "Home Page";
}

<div class="content">
    我是中间内容--@ViewData["Message"]
    <ul>
        @if (Model.Count > 0)
        {
            @foreach (var item in Model)
            {
                <li class="badge">@item.UserName</li>
            }
        }
    </ul>
</div>